# Architecture

This repository uses a compact learning-to-project flow:

```text
src/        reusable Python modules
examples/   runnable demonstrations
tests/      automated checks
labs/       scenario-based practice
projects/   portfolio applications
```

The current first working implementation lives in `src/` and is demonstrated from `examples/`.
